#include <stdio.h>
#include <math.h>
/* Include other headers as needed */
int main()
{
   int a,b;
  scanf("%d\n",&a);
  scanf("%d\n",&b);
  int c=pow(a,b);
  printf("%d",c);
  
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}